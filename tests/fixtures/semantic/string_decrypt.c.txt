int decode_string(const uint8_t *enc, size_t n, uint8_t key, char *out) {
    for (size_t i = 0; i < n; ++i) {
        out[i] = (char)(enc[i] ^ key);
        key = (uint8_t)((key << 1) | (key >> 7));
        key = (uint8_t)(key + (uint8_t)i);
    }
    out[n] = '\0';
    return 0;
}
