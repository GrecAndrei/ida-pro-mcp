repos:
  - repo: local
    hooks:
      - id: test-registry-check
        name: Test Binding Registry Check
        entry: python scripts/test_registry_check.py --strict
        language: python
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
