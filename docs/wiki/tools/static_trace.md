# static_trace (removed)

This tool no longer exists as a first-class MCP tool.

Use instead:
- `trace_analysis` for dynamic/trace-oriented workflows
- `code` / `search` for static path and string work
- `emulate` (Tier B, unadvertised) if present in your build

See `docs/ROADMAP.md`.
